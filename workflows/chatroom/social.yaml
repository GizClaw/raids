---
apiVersion: gizclaw.admin/v1alpha1
kind: Workflow
metadata:
  name: chatroom
spec:
  driver: chatroom
  chatroom:
    history:
      ttl: 168h
    transcript:
      enabled: true
      asr_model: asr
