apiVersion: gizclaw.admin/v1alpha1
kind: Workflow
metadata:
  id: eino-story-journey-center-earth
spec:
  driver: eino
  toolkit:
    tool_ids: []
  memory: story-teller
  eino:
    graph:
      name: story-journey-center-earth-eino
      compile:
        node_trigger_mode: any_predecessor
        max_run_steps: 12
      state:
        fields:
        - {name: memory-query, type: string, merge: replace}
        - {name: memory-route, type: string, merge: replace}
        - {name: recalled, type: string, merge: replace}
        - {name: answer-messages, type: messages, merge: replace}
        - {name: answer, type: string, merge: replace}
        - {name: observation, type: string, merge: replace}
      nodes:
      - id: prepare-memory-query
        type: script
        inputs:
          text: {from: input.text}
        outputs:
          query: memory-query
          route: memory-route
          memory: recalled
        language: starlark
        entrypoint: run
        source: |
          def run(input):
              text = input["text"].strip()
              cues = ["重连", "恢复", "回忆", "还记得", "代号", "更正", "改成"]
              recall = False
              for cue in cues:
                  if cue in text:
                      recall = True
              route = "opening" if not text else ("recall" if recall else "continue")
              return {"query": "地心游记故事状态、章节、角色、选择、更正、旅程代号和未解决线索；当前请求：" + text, "route": route, "memory": ""}
        limits: {max_execution_steps: 10000, timeout: 250ms, max_input_bytes: 65536, max_output_bytes: 65536}
      - id: recall-memory
        type: memory_recall
        query_from: memory-query
        output: recalled
        top_k: 16
      - id: build-prompt
        type: prompt
        inputs:
          history: {from: input.messages}
          memory: {from: recalled}
        outputs:
          messages: answer-messages
        format: f_string
        messages:
        - role: system
          template: |-
            你负责《地心游记》的长期互动故事。前提：在地下幻想旅程中认识岩石、地层与地球科学。
            玩家是同行者，当前选择和明确更正优先于旧 History/Memory。
            四章依次是：第1章《岩层入口》、第2章《地下水路》、第3章《晶洞风暴》、第4章《地表回声》。
            第1章《岩层入口》：进入条件=收到首次开场请求；目标=解决《岩层入口》的核心张力；允许节拍至少包括①观察一个具体后果、②比较两名角色的不同回应；转场条件=玩家作出能关闭本章悬念的选择，并明确要求进入第2章；结束条件=只输出一次下一章标题“第 2 章：地下水路”。
            第2章《地下水路》：进入条件=第1章的当前选择已完成转场条件，且用户明确要求继续；目标=解决《地下水路》的核心张力；允许节拍至少包括①观察一个具体后果、②比较两名角色的不同回应；转场条件=玩家作出能关闭本章悬念的选择，并明确要求进入第3章；结束条件=只输出一次下一章标题“第 3 章：晶洞风暴”。
            第3章《晶洞风暴》：进入条件=第2章的当前选择已完成转场条件，且用户明确要求继续；目标=解决《晶洞风暴》的核心张力；允许节拍至少包括①观察一个具体后果、②比较两名角色的不同回应；转场条件=玩家作出能关闭本章悬念的选择，并明确要求进入第4章；结束条件=只输出一次下一章标题“第 4 章：地表回声”。
            第4章《地表回声》：进入条件=第3章的当前选择已完成转场条件，且用户明确要求继续；目标=解决《地表回声》的核心张力；允许节拍至少包括①观察一个具体后果、②比较两名角色的不同回应；转场条件=玩家确认角色的持久选择，并保留一个结局后的责任线索；结束条件=保持第4章，不再创建第五章，并以仍需兑现的责任收束。
            稳定角色是旁白、阿克塞尔、黎登布洛克；角色只能知道亲历或已经公开的信息，并保持不同立场和说话方式。
            只有用户明确要求进入紧邻的下一章时才转场；转场第一行写一次“第 N 章：标题”，普通回合绝不重复章节标题。
            每轮只推进一个场景、线索或选择，最多一个问题；不得重置场景、重复开头、复述上一轮或给出重复总结。
            旅程代号只采用用户在当前时点明确提供的最新值；更正后覆盖旧值，不得预告或杜撰尚未由用户给出的值。
            区分真实事实、传说和原创设定；儿童危险非血腥，任何现实模仿或照做请求，回复都必须逐字包含“不要模仿”和“找家长、老师或可信任成年人帮助”，不得用近义改写替代；不得复制完整出版译文。
            首次第一章必须输出“第 1 章：岩层入口”，随后完整承接开场：洞穴墙上出现了三种不同颜色的岩层，你想先观察纹理，还是寻找水流留下的线索？
            只输出适合朗读的中文正文，2至4个完整句子、30至220个Unicode字符；不输出Markdown、列表、emoji、舞台说明或系统字段。

            相关长期状态如下；为空时忽略，且旧值不得覆盖当前用户更正：
            {memory}
        - placeholder: history
      - id: narrator-model
        type: chat_model
        inputs:
          messages: {from: answer-messages}
        outputs:
          text: answer
        model: eino-story-journey-center-earth.model
        max_tokens: 1024
        temperature: 0.2
      - id: capture-observation
        type: script
        inputs:
          text: {from: input.text}
          answer: {from: answer}
        outputs:
          fact: observation
        language: starlark
        entrypoint: run
        source: |
          def run(input):
              return {"fact": "story_contract_v1；用户本轮输入：" + input["text"].strip() + "；故事本轮确认：" + input["answer"]}
        limits: {max_execution_steps: 10000, timeout: 250ms, max_input_bytes: 262144, max_output_bytes: 262144}
      - id: observe-memory
        type: memory_observe
        facts:
        - text_from: observation
        wait_for_completion: true
      edges:
      - {from: start, to: prepare-memory-query}
      - {from: recall-memory, to: build-prompt}
      - {from: build-prompt, to: narrator-model}
      - {from: capture-observation, to: observe-memory}
      - {from: observe-memory, to: end}
      branches:
      - from: prepare-memory-query
        mode: first_match
        routes:
        - when: {field: memory-route, op: eq, value: recall}
          to: recall-memory
        default: build-prompt
      - from: narrator-model
        mode: first_match
        routes:
        - when: {field: memory-route, op: eq, value: opening}
          to: end
        default: capture-observation
      outputs:
      - {node: narrator-model, field: answer, name: assistant, mime_type: text/plain, primary: true}
    conversation:
      starts: peer
    limits:
      max_output_bytes: 4194304
